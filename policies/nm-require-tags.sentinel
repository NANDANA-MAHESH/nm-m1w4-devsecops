import "tfplan/v2" as tfplan

tagged_resources = filter tfplan.resource_changes as _, rc {
  rc.mode is "managed" and
  (rc.change.after else {}) contains "tags"
}

main = rule {
  length(tagged_resources) > 0
}
